{% extends 'base.html.twig' %}
{% block title %} <title> IngMin {{parent()}} </title> {% endblock %}
{% block body %}
    {% block content %}
        <h1>Liste des burgers avec au moins {{ numMin }} ingrédients</h1>
        <ul>
        {% if burgers|length > 0 %} {# vérifie que le tableau n'est pas vide, s'il l'est on indique à l'utilisateur que rien ne correspond à sa recherche #}
            {% for id, burger in burgers %}            
                <li><a>{{ burger.nom }}</a></li>
            {% endfor %}
        {% else %}
            Aucun burger ne contient autant d'ingrédients !
        {% endif %}
        </ul>
    {% endblock %}
{% endblock %}